# Eidos minimal adoption target

Current `EcExperienceProposalV010` remains authoritative.

1. Keep EC Proposal validation inside Eidos.
2. Add/confirm a versioned host `ActionRequest` boundary.
3. Eidos must never execute enterprise state mutation itself.
4. Host/EVO re-authorizes every action.
5. Do not create long-term user/enterprise learning inside Eidos.
